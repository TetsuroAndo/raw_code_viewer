# raw_code_viewer

コードベースのディレクトリ構造とファイル内容を、コメントを除去して表示するBashスクリプトです。

## 機能

- ディレクトリツリーの表示（`tree`コマンドを使用）
- コードファイルの内容をコメント除去して表示
- テストファイルの除外オプション
- `.gitignore`に基づく自動除外

## セットアップ

```bash
./setup.sh
```

PATHに含まれるbinディレクトリに`rawcode`コマンドとしてシンボリックリンクを作成します。
